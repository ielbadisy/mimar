% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_prep.R
\name{nelsonaalen}
\alias{nelsonaalen}
\title{Nelson-Aalen cumulative hazard as an imputation predictor}
\usage{
nelsonaalen(
  data,
  timevar,
  statusvar,
  aux = NULL,
  method = c("auto", "strata", "breslow")
)
}
\arguments{
\item{data}{A data frame.}

\item{timevar}{Follow-up time. A column name given as a string or as an
unquoted symbol.}

\item{statusvar}{Event indicator, \code{1}/\code{TRUE} for an event and \code{0}/\code{FALSE}
for censoring. A column name given as a string or as an unquoted symbol.}

\item{aux}{Optional character vector of auxiliary column names used to make
the estimate conditional (strata for categorical variables, Cox
covariates otherwise).}

\item{method}{How \code{aux} is used: \code{"auto"} (the default), \code{"strata"} for
stratified estimation, or \code{"breslow"} for a Cox model with a
Nelson-Aalen (Breslow) baseline.}
}
\value{
A numeric vector, \code{nrow(data)} long, giving the cumulative hazard at
each subject's time. Rows with a missing time or status (or missing \code{aux},
when \code{aux} is supplied) yield \code{NA}.
}
\description{
\code{nelsonaalen()} returns the Nelson-Aalen estimate of the cumulative hazard
\eqn{\hat H(t)} evaluated at each subject's event or censoring time.
Following White and Royston (2009), the cumulative hazard together with the
event indicator is the recommended way to carry time-to-event information
into the imputation model for other, partially observed variables: the raw
survival time must never be used as a predictor, as it biases the imputed
values.
}
\details{
With no \code{aux}, the marginal Nelson-Aalen estimator is used, matching
\code{mice::nelsonaalen()}. When \code{aux} names one or more auxiliary variables the
estimate is made conditional on them: \code{method = "strata"} estimates a
separate cumulative hazard within each combination of the (categorical)
\code{aux} variables, while \code{method = "breslow"} fits a Cox model on \code{aux} and
returns the per-subject cumulative hazard
\eqn{\hat H_0(t)\exp(x_i^\top\hat\beta)} (this path requires the
\pkg{survival} package). The default, \code{method = "auto"}, picks \code{"strata"}
when every \code{aux} variable is categorical and \code{"breslow"} otherwise.
}
\examples{
set.seed(1)
n <- 120
df <- data.frame(
  sex = factor(sample(c("F", "M"), n, TRUE)),
  bmi = rnorm(n, 26, 4)
)
event_time <- rexp(n, rate = 1 / 8)
cens_time <- rexp(n, rate = 1 / 12)
df$time <- pmin(event_time, cens_time)
df$status <- as.integer(event_time <= cens_time)

## add H0 as a predictor to use in place of the raw survival time
df$H0 <- nelsonaalen(df, "time", "status")
df$H0_bysex <- nelsonaalen(df, time, status, aux = "sex")

df$bmi[sample(n, 25)] <- NA
impute(df[c("bmi", "sex", "status", "H0")], m = 5, imputer = "pmm", seed = 1)
}
\references{
White, I. R. and Royston, P. (2009). Imputing missing covariate
values for the Cox model. \emph{Statistics in Medicine}, 28(15),
1982-1998.
}
\seealso{
\code{\link[=ipcw]{ipcw()}}
}
