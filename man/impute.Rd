% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/impute.R
\name{impute}
\alias{impute}
\alias{impute.data.frame}
\alias{impute.mimar_amputation}
\title{Impute missing data}
\usage{
impute(x, ...)

\method{impute}{data.frame}(x, m = 5, imputer = "pmm", maxit = 5, seed = NULL, ncore = 1, ...)

\method{impute}{mimar_amputation}(x, m = 5, imputer = "pmm", maxit = 5, seed = NULL, ncore = 1, ...)
}
\arguments{
\item{x}{A data frame or \code{mimar_amputation} object.}

\item{...}{Passed to methods.}

\item{m}{Number of completed data sets to generate.}

\item{imputer}{Imputer name. Any name returned by \code{imputer_registry()} can
be supplied to use that imputer for all incomplete variables it supports.}

\item{maxit}{Number of chained-equation iterations.}

\item{seed}{Optional random seed.}

\item{ncore}{Number of CPU cores used to run completed datasets in
parallel. The default, \code{1}, runs sequentially. Values greater than one are
passed to \code{functionals::fmap()} as \code{ncores}.}
}
\value{
A \code{mimar_imputation} object.
}
\description{
\code{impute()} performs single or multiple imputation through a chained update
procedure owned by \code{mimar}. The default \code{imputer = "pmm"} uses predictive
mean matching. A named imputer such as \code{"naive"}, \code{"rf"}, \code{"xgboost"},
\code{"knn"}, or \code{"glmnet"} can also be supplied to use that learner for every
incomplete variable it supports. The returned
object keeps completed datasets first; use \code{complete()} to extract one
completed dataset, all completed datasets, or a stacked long data frame.
}
\details{
Let \eqn{X_j} be an incomplete variable and \eqn{X_{-j}} all remaining
variables. At each iteration, \code{mimar} fits an imputer learner on observed rows
\deqn{\hat f_j: X_{-j}^{obs} \rightarrow X_j^{obs}}
and predicts missing cells \eqn{X_j^{mis}} from \eqn{X_{-j}^{mis}}. This is
repeated across incomplete variables for \code{maxit} iterations and across
\eqn{m} independent completed data sets. The algorithm is intentionally
learner-agnostic: each imputer is constructed with \code{imputer()}, trained with
\code{fit()}, and used through \code{predict()}.

Each requested imputer is applied to all incomplete variables it supports.
Use \code{imputer_registry()} to inspect target-type compatibility.
}
\section{Methods (by class)}{
\itemize{
\item \code{impute(data.frame)}: Impute a data frame.

\item \code{impute(mimar_amputation)}: Impute a \code{mimar_amputation} object and retain truth masks
for later evaluation.

}}
