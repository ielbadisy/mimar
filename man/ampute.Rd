% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ampute.R, R/generics.R
\name{ampute.data.frame}
\alias{ampute.data.frame}
\alias{ampute}
\title{Artificially introduce missing data}
\usage{
\method{ampute}{data.frame}(
  x,
  prop = 0.1,
  mechanism = c("MCAR", "MAR", "MNAR"),
  target = NULL,
  by = NULL,
  direction = c("both", "left", "right"),
  seed = NULL,
  ...
)

ampute(x, ...)
}
\arguments{
\item{x}{A data frame.}

\item{prop}{Target marginal proportion of newly introduced missing values.}

\item{mechanism}{Missingness mechanism: missing completely at random
(\code{"MCAR"}), missing at random (\code{"MAR"}), or missing not at random
(\code{"MNAR"}).}

\item{target}{Character vector of variables eligible for amputation. Defaults
to all variables.}

\item{by}{Character vector of fully or partially observed variables used to
drive MAR probabilities. Required for \code{mechanism = "MAR"}.}

\item{direction}{For MNAR numeric targets, whether missingness is more likely
in both tails, the left tail, or the right tail.}

\item{seed}{Optional random seed.}

\item{...}{Passed to methods.}
}
\value{
A \code{mimar_amputation} object.
}
\description{
\code{ampute()} creates benchmark data by adding missing values under MCAR, MAR,
or MNAR mechanisms. The returned object keeps the original data, amputated
data, and masks for original, added, and total missingness.
}
\details{
For each target variable \eqn{X_j}, observed cells are removed by drawing
\deqn{A_{ij} \sim \mathrm{Bernoulli}(p_i)}
and setting \eqn{X_{ij}} to missing when \eqn{A_{ij}=1}. For MCAR,
\eqn{p_i = \pi}, where \eqn{\pi} is \code{prop}. For MAR, probabilities are based
on observed \code{by} variables through a standardized score \eqn{s_i}:
\deqn{p_i = \mathrm{logit}^{-1}(\alpha + s_i),}
with \eqn{\alpha} calibrated so that the average probability is \code{prop}. For
MNAR, \eqn{s_i} is derived from the target variable itself; \code{direction}
selects high values, low values, or both tails for numeric targets.
}
